{
  "name": "codex-team",
  "version": "0.2.0",
  "private": true,
  "description": "Claude-managed team of long-lived Codex app-server sessions",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup src/main.ts --format cjs --platform node --target node18 --out-dir dist --clean",
    "test": "tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "smol-toml": "^1.4.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.3",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
